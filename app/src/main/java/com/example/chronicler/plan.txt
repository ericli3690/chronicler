CURRERNT

- send to ahyan, kevin for feedback
- actually run the test plan in crit b
- refactor carddate leap year checker so it only runs if month = 2
- import menu
    - preinstalled decks
    - api fetching
    - open files
        - make sure this handles dates in the future with an error
- export files

POLISH
- document all code
- handle all todos

NOTES
- if a safeargs error happens again:
    - delete all actions in the navgraph visual ui
    - rebuild
    - reconnect all actions in the navgraph visual ui
    - rebuild
- filemanager writing an array has never been tested